Node module for creating seeder/pseudo seeder/peer type node in the network
Using chain for sending metadata through out the node-spread.
Network space for each node, from each node with comparable access time.

Keep thinking and expand this.
